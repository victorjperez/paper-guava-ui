import { Story, Meta, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Button } from './Button';

<Meta title="Components/Button" component={Button} />

# Button

`Button` is the basic interactive element.

export const Template = (args) => <Button {...args} />

<Canvas>
<Story 
  name="Main Story"
  args={{
    label: 'Button',
    size: 'md'
}}>
  {Template.bind({})}
</Story>
</Canvas>

<Canvas>
<Story 
  name="Green"
  args={{
    accentColor: 'forestgreen',
    label: 'Green Button',
}}>
  {Template.bind({})}
</Story>
</Canvas>

<ArgsTable of={Button} />