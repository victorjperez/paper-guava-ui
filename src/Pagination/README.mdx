import { Story, Meta, Canvas, ArgsTable } from '@storybook/addon-docs';
import { StorybookDocsFooter } from '../StorybookDocsFooter';

import { Pagination } from './Pagination';

# Pagination

WARNING: THIS COMPONENT IS A WORK IN PROGRESS.

`Pagination` description here. This should be brief and give an accurate overview of this component's functionality.

<Canvas>
  <Story id="core-ui-pagination--main-story" />
</Canvas>

```jsx
import React from 'react';
import { Pagination } from '@pguava-ui';

<Pagination />;
```

## Component Options

<ArgsTable of={Pagination} />

Below is a more detailed list of props for `Pagination` and their usage.

### Prop

This is a in-depth description of a prop.

```jsx
<Pagination prop={true} />
```

## Accessibility

Below are the built in accessibility notes and considerations for `Pagination`.

### Accessibility Note

If there are any special accessibility concerns to note, provide them here in depth.

<StorybookDocsFooter />
