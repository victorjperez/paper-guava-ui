import { Story, Meta, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Tile } from './Tile';

<Meta title="Components/Tile" component={Tile} />

# Tile

`Tile` is a component that renders a label with an icon above it. If provided with a `link` it acts as an anchor instead of a button.

export const Template = (args) => <Tile {...args} />


<Canvas>
<Story 
  name="Main Story"
  args={{
    label: 'Tile',
    tileIcon: <span style={{ padding: '8px 0', fontSize: '2em' }}>🤨</span>,
}}>
  {Template.bind({})}
</Story>
</Canvas>

<Canvas>
<Story 
  name="Accented"
  args={{
    label: 'Tile',
    styleType: 'accented',
    tileIcon: <span style={{ padding: '8px 0', fontSize: '2em' }}>🤨</span>,
}}>
  {Template.bind({})}
</Story>
</Canvas>

<ArgsTable of={Tile} />